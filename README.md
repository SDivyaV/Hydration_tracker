# Daily Water Intake Tracker (Project Skeleton)

This is a Spring Boot project skeleton for tracking daily water intake.

Requirements:
- Java 18
- Maven

Build:

```bash
mvn clean package
```

Run:

```bash
mvn spring-boot:run
```

H2 console is enabled at `/h2-console`.
